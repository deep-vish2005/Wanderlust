<nav class="navbar navbar-expand-md navbar-light bg-white sticky-top custom-navbar">
  <div class="container">

    <!-- Logo -->
    <a class="navbar-brand d-flex align-items-center gap-2 fw-bold" href="/listings">
      <div class="logo-icon">
        <i class="fa-solid fa-compass"></i>
      </div>
      <span>Roamingo</span>
    </a>

    <!-- Toggle -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">

      <!-- Center links -->
      <ul class="navbar-nav mx-auto gap-md-4 align-items-center">

  <!-- Explore -->
  <li class="nav-item">
    <a class="nav-link nav-hover" href="/listings">Explore</a>
  </li>

  <!-- Add Listing -->
  <li class="nav-item">
    <a class="nav-link nav-hover" href="/listings/new">Add Listing</a>
  </li>

  <!-- Search Bar -->
  <li class="nav-item">
    <form class="search-bar d-flex">
      <input 
        type="search" 
        class="form-control search-input" 
        placeholder="Search destinations"
      >
      <button class="search-btn" type="submit">
        <i class="fa-solid fa-magnifying-glass"></i>
      </button>
    </form>
  </li>

</ul>

      <!-- Right side -->
      <div class="d-flex align-items-center gap-3">

        <% if(!currUser) { %>
          <a class="nav-link nav-hover" href="/signup">Sign up</a>
          <a class="btn btn-dark rounded-pill px-3" href="/login">Login</a>
        <% } %>

        <% if(currUser) { %>
          <span class="user-text">Hi, <%= currUser.username %></span>
          <a class="btn btn-outline-dark rounded-pill px-3" href="/logout">Logout</a>
        <% } %>

      </div>

    </div>
  </div>
</nav>
